# Build output
target/
*.class

# Runtime artifacts generated by the apps (not part of the source you download)
logs/*.json
logs/*.json.gz
checkpoint/

# IDE noise (we deliberately KEEP .idea/runConfigurations so the Run configs ship with the project)
.idea/workspace.xml
.idea/tasks.xml
.idea/shelf/
*.iml
out/

# OS noise
.DS_Store
Thumbs.db
