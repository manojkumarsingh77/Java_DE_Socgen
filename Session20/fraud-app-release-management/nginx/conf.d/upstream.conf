# This file is REWRITTEN by scripts/08-canary-rollout.sh and
# scripts/09-rollback.sh - do not hand-edit during a demo.
#
# Initial state: 100% of traffic on the blue (current stable) slot; green
# (release candidate) is registered but marked "down" so it never receives
# live traffic until the canary rollout explicitly ramps it up.
upstream fraud_backend {
    server fraud-app-blue:8080;
    server fraud-app-green:8080 down;
}
